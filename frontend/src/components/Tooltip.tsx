/**
 * Re-exports the shadcn Tooltip primitives.
 * Kept as a barrel so existing import paths (@/components/Tooltip) work unchanged.
 */
export {
  Tooltip,
  TooltipTrigger,
  TooltipContent,
  TooltipProvider,
} from "@/components/ui/tooltip";
