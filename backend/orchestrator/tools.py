"""Tool definitions available to the recommendation agent (not yet implemented).

Tools are callable units the agent invokes during a turn — for example,
querying the movie database, fetching user preference history, or calling an
embedding API.  Each tool will be defined here and registered with the agent
in ``backend/orchestrator/agent.py``.
"""
