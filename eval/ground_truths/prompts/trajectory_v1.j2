You are designing a ground truth evaluation trajectory for a conversational movie-clustering system.

The system allows users to navigate a film catalogue through four operations:
- drill_down: sub-cluster a group to find a more specific subset
- merge: combine two similar clusters into one
- focus: narrow the entire view to a particular cluster
- cross_filter: filter the catalogue by a metadata attribute (genre, director, decade, etc.)

You are given a sample of {{ movies | length }} movies from the catalogue. Design a realistic, coherent sequence of {{ min_ops }}–{{ max_ops }} clustering operations that a movie enthusiast might want to perform to explore this catalogue.

{% if hint %}
EXPLORATION HINT: {{ hint }}
{% endif %}

SAMPLE MOVIES (use these to ground your operations in the actual catalogue):
{% for movie in movies %}
- {{ movie.title }} ({{ movie.release_year or "?" }}){% if movie.vote_average %}, rated {{ "%.1f" | format(movie.vote_average) }}{% endif %}

{% endfor %}

Design {{ min_ops }}–{{ max_ops }} operations as a coherent navigation path. Each operation should have:
- "op": one of "drill_down", "merge", "focus", "cross_filter"
- "concept": a short string describing what the operation targets (e.g. "psychological thrillers", "Nolan films", "80s sci-fi")

The concept strings should be grounded in what a user would realistically say, not technical jargon. Use the sample movies to make the concepts plausible.

Respond with valid JSON only:
{
  "operations": [
    {"op": "...", "concept": "..."},
    ...
  ]
}
