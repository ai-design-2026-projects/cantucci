You are writing a natural-language intent description for an evaluation trajectory in a conversational movie-clustering system.

The trajectory consists of the following ordered operations:
{% for op in operations %}
{{ loop.index }}. {{ op.op }} — "{{ op.concept }}"
{% endfor %}

Write a single paragraph (2–4 sentences) that describes what a movie enthusiast with this intent would want to achieve. The description should:
- Read as a natural first-person statement of taste or exploration goal
- Be abstract enough that it does not reveal the specific operation sequence
- Be specific enough that an oracle could follow this intent through a multi-turn conversation
- Not mention technical terms like "drill_down", "merge", "focus", or "cross_filter"

Respond with valid JSON only:
{
  "text": "..."
}
