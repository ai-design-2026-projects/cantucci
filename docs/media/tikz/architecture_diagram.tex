\documentclass[tikz, border=20pt]{standalone}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, shadows, fit, backgrounds, calc}

\begin{document}
\pagecolor{white}
\begin{tikzpicture}[
    font=\sffamily,
    >=Stealth,
    node distance=1.5cm and 2.5cm,
    every node/.style={align=center},
    user/.style={circle, draw=cyan!80!black, fill=cyan!15, thick, minimum size=2.2cm},
    db/.style={cylinder, shape border rotate=90, draw=blue!70!black, fill=blue!10, thick, aspect=0.25, minimum width=4.5cm, minimum height=1.8cm},
    judge/.style={rectangle, draw=red!80!black, fill=red!10, thick, rounded corners=3mm, minimum width=4cm, minimum height=1.3cm},
    cluster/.style={rectangle, draw=purple!70!black, fill=purple!10, thick, rounded corners=3mm, minimum width=2.6cm, minimum height=1.5cm},
    orchestrator/.style={rectangle, draw=orange!80!black, fill=orange!15, thick, rounded corners=3mm, minimum width=2.6cm, minimum height=1.5cm},
    decision/.style={rectangle, draw=teal!70!black, fill=teal!15, thick, rounded corners=3mm, minimum width=2.6cm, minimum height=1.5cm},
    retrieval/.style={rectangle, draw=teal!70!black, fill=teal!15, thick, rounded corners=2mm, minimum width=3.4cm, minimum height=1.1cm},
    state/.style={rectangle, draw=yellow!70!black, fill=yellow!10, thick, rounded corners=3mm, minimum width=2.6cm, minimum height=1.5cm},
    profile/.style={rectangle, draw=green!60!black, fill=green!10, thick, rounded corners=3mm, minimum width=2.6cm, minimum height=1.5cm},
    llm_node/.style={draw=gray!80, fill=white, thick, inner sep=3pt, font=\bfseries\small, rounded corners=1mm},
    arrow/.style={->, thick, color=black!70},
    biarrow/.style={<->, thick, color=black!70},
    judge_arrow/.style={->, dashed, thick, color=red!60!black}
]

% ==================== NODES ====================

\node[orchestrator] (orchestrator) {\textbf{Orchestrator}};

\node[user] (user) at ([yshift=5cm]orchestrator) {\textbf{Oracle}};

\node[db] (db) at ([xshift=16cm, yshift=-3.5cm]orchestrator)
    {\textbf{Database}\\\footnotesize Movie \& Session Data};

% Parallel wave row
\node[state]    (state)    at ([xshift=-5cm,  yshift=-3.5cm]orchestrator) {\textbf{State Agent}};
\node[llm_node, anchor=south west] at ([xshift=-10pt, yshift=-7pt]state.south west)    {LLM};

\node[profile]  (profile)  at ([xshift=-1.5cm, yshift=-3.5cm]orchestrator) {\textbf{Profile Agent}};
\node[llm_node, anchor=south west] at ([xshift=-10pt, yshift=-7pt]profile.south west)  {LLM};

\node[retrieval] (retrieval) at ([xshift=9cm, yshift=-3.5cm]orchestrator) {\textbf{Retrieval System}};
\node[llm_node, anchor=south west] at ([xshift=-10pt, yshift=-7pt]retrieval.south west) {LLM};

% Lower row
\node[cluster]  (cluster)  at ([xshift=3cm,  yshift=-3.5cm]orchestrator) {\textbf{Cluster Agent}};
\node[llm_node, anchor=south west] at ([xshift=-10pt, yshift=-7pt]cluster.south west)  {LLM};

\node[decision] (decision) at ([xshift=0cm, yshift=-3cm]profile) {\textbf{Decision Agent}};
\node[llm_node, anchor=south west] at ([xshift=-10pt, yshift=-7pt]decision.south west) {LLM};

% ==================== BACKGROUND ====================

\coordinate (agentsWestPad) at ([xshift=-3cm]state.west);
\begin{scope}[on background layer]
    \node[fill=gray!3, rounded corners=5mm, draw=gray!50, dashed, thick,
        fit=(agentsWestPad)(state)(profile)(retrieval)(cluster)(decision)(orchestrator),
        inner sep=15pt] (agents) {};
    \node[anchor=north west, font=\bfseries, color=gray!60]
        at ([xshift=8pt, yshift=-4pt]agents.north west) {Agentic Logic Layer};
\end{scope}

% ==================== CONNECTIONS ====================

% Oracle <-> Orchestrator
\draw[->] ([xshift=-0.2cm]user.south) -- ([xshift=-0.2cm]orchestrator.north)
    node[left, pos=0.5, font=\footnotesize] {Feedback};
\draw[->] ([xshift=0.2cm]orchestrator.north) -- ([xshift=0.2cm]user.south)
    node[right, pos=0.5, font=\footnotesize] {Show, Ask, Stop};

% Parallel wave: Orchestrator dispatches State, Profile, Retrieval concurrently
\coordinate (orchestratorSouthPad) at ([yshift=-0.7cm]orchestrator.south);
\draw[arrow] (orchestrator.south) -| (orchestratorSouthPad) -| (state.north) node[left, yshift=-10pt, pos=0.5, font=\footnotesize] {Event Detection};
\draw[arrow] (orchestrator.south) -| (orchestratorSouthPad) -| (profile.north) node[left, yshift=-10pt, pos=0.5, font=\footnotesize] {Extraction};
\draw[arrow] (orchestrator.south) -| (orchestratorSouthPad) -| (retrieval.north) node[left, yshift=-10pt, pos=0.5, font=\footnotesize] {Retrieval};
\draw[arrow] (orchestrator.south) -| (orchestratorSouthPad) -| (cluster.north) node[left, yshift=-10pt, pos=0.5, font=\footnotesize] {Refine};

% Retrieval -> Cluster (candidates)
\draw[arrow] (retrieval.west) -- (cluster.east)
    node[below, pos=0.5, font=\footnotesize] {Candidates};

% State -> Orchestrator (events)
\coordinate[xshift=-2cm] (stateWestPad) at (state.west);
\coordinate[xshift=-7cm, yshift=-0.25cm] (orchestratorWestPad) at (orchestrator.west);
\draw[arrow] (state.west) -- node[below, pos=0.5, font=\footnotesize] {Events} (stateWestPad) -- (orchestratorWestPad) -- ([yshift=-0.25cm]orchestrator.west);


% Profile -> Decision (profile data)
\draw[arrow] (profile.south) -- (decision.north)
    node[left, pos=0.5, font=\footnotesize] {Profile Data};

% Retrieval <-> Database (vector search, same y-level)
\draw[biarrow] (retrieval.east) -- (db.west)
    node[above, pos=0.5, font=\footnotesize] {Vector Search};

% Cluster -> Decision (clusters)
\draw[arrow] (cluster.south) |- (decision.east)
    node[right, pos=0.2, font=\footnotesize] {Clusters};

% Decision -> Orchestrator (ASK / SHOW) via far-left margin
\draw[arrow] (decision.west) -- node[above, xshift=-1cm, near start, font=\footnotesize] {Recommend / Question} ++(-6cm, 0) |-
    (orchestrator.west);

% Orchestrator -> Database (session writes)
\draw[arrow] (orchestrator.east) -| (db.north)
    node[right, pos=0.7, font=\footnotesize] {Write\\Session};

\end{tikzpicture}
\end{document}
