# Public API and Internal Interfaces

The named contracts between modules (and with external callers or systems): inputs, outputs, error cases. These are the handoffs that ablation and multi-person work depend on.